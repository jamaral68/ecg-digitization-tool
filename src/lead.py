class Lead:
    def __init__(self, name, coords):
        """
        name: str - nome da lead (ex: 'I', 'V1')
        coords: list[int] - [x1, y1, x2, y2]
        """
        self.name = name
        self.coords = coords
